Your new match below:

<%= @partner.nickname %> <br>
<%= image_tag @partner.image_url,  %>

<%= button_to "Pair", paired_path(partner_id: @partner.id)  %>

<%= button_to "Unpair", unpaired_path(partner_id: @partner.id) %>

<%= link_to "Back to Your Dashboard", dashboard_path %>
