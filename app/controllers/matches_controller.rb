class MatchesController < ApplicationController
  def index
    # @users = User.different_users

  end

  private

  # def different_users
  #   current_user = User.find_by
  #   User.find(:all, :conditions => ["id != ?", current_user.id])
  # end
end
